[project]
name = "file-backuper"
version = "1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
backuper = "backuper_app.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backuper_app"]